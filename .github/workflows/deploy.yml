name: deployment

on:
  push:
    branches:
      - master
    paths:
      - 'workspace-client/**'

jobs:
  workspace-client-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'
      
      - name: Install dependencies
        run: |
          cd workspace-client
          npm install

      - name: build the project
        run: |
          cd workspace-client
          npm run build
        
      - name: Deploy to S3
        env: 
          AWS_ACCESS_KEY_ID: ${{secrets.AWS_ACCESS_KEY_ID}}
          AWS_SECRET_ACCESS_KEY: ${{secrets.AWS_SECRET_ACCESS_KEY}}
          AWS_REGION: ${{secrets.AWS_REGION}}
        run:
          aws s3 sync workspace-client/dist s3://${{secrets.S3_BUCKET_NAME}} --delete